var React = require('react');

var About = React.createClass({
  render: function () {
    return (
      <div>
        This is where we are about-ing
      </div>
    );
  }
});

module.exports = About;
